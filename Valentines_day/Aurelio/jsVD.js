function abrirSobre() { // Función para abrir el sobre
    document.querySelector('.container').classList.toggle('abierto'); // Selecciona el primer elemento que coincida con el selector y le añade o quita la clase 'abierto'
}